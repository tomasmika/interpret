from setuptools import setup, find_packages

setup(
    name='interpret',
    version='0.5.0',  # You can adjust the version number as needed
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)
